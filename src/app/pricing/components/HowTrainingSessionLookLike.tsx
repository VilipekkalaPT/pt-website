import { HOW_TRAINING_SESSION_LOOKS_LIKE } from "app/utils/variables";

export default function HowTrainingSessionLookLike() {
  return (
    <div className="mt-25 px-12 w-full">
      <p className="text-2xl font-bold">{HOW_TRAINING_SESSION_LOOKS_LIKE}</p>
    </div>
  );
}
